import React from 'react';
import { StyleSheet, View } from "react-native";
import { IconButton, Avatar, Icon, Fab, Input, Text, Box, HStack, Center, NativeBaseProvider, VStack, Image, Pressable } from "native-base";

const Map = () =>{
    return (
        <NativeBaseProvider>
            <Text>This will soon render a complete map! 🗺</Text>
        </NativeBaseProvider>
    );
};

export default Map;